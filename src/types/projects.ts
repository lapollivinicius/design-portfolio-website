export type project = {
  id: number,
  title: string,
  slug: string,
  year: number,
  copy: string,
  category: string,
  typograph: string,
  description: string,
  solution: string,
  services: string[],
  image: string,
  video: string
}